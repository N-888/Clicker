# -*- coding: utf-8 -*-
# Указываем кодировку UTF-8 для поддержки русских символов

"""
Точка входа для запуска Flask приложения.
Этот файл запускает веб-сервер для разработки.
Для продакшена используется WSGI сервер.
"""

# Импортируем функцию create_app и db из пакета app
from app import create_app, db

# create_app: фабричная функция для создания приложения
# db: объект базы данных

# Создаем экземпляр Flask приложения
app = create_app()
# Вызываем фабричную функцию, которая возвращает настроенное приложение

# Создаем контекст приложения для работы с базой данных
with app.app_context():
    """
    Контекст приложения необходим для выполнения операций с базой данных.
    Все операции с базой данных должны выполняться внутри контекста приложения.
    """

    # Импортируем модели, чтобы они были известны SQLAlchemy
    from app.models import User

    # User: модель пользователя
    # Импорт делаем здесь, чтобы избежать циклических импортов

    # Создаем все таблицы в базе данных, если они еще не существуют
    db.create_all()
    # Эта команда создает таблицы на основе моделей, определенных в models.py
    # Если таблицы уже существуют, ничего не происходит (не перезаписывает)

    # ВАЖНО: После первого запуска приложения
    # эту строку можно закомментировать (# db.create_all())
    # чтобы не пытаться создавать таблицы при каждом запуске

# Проверяем, запущен ли этот файл напрямую (а не импортирован)
# if __name__ == '__main__':
    """
    Этот блок выполняется только при прямом запуске файла: python main.py
    При импорте этого файла как модуля, этот блок НЕ выполняется.
    """

    # Запускаем Flask приложение в режиме отладки
    # app.run(debug=True, port=5000)
    # run(): запускает встроенный веб-сервер Flask (только для разработки!)
    # debug=True: режим отладки (автоматическая перезагрузка при изменениях кода)
    # port=5000: порт, на котором будет работать сервер (http://localhost:5000)